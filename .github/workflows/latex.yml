name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Setup
      run: |
        bash <(curl -sL https://2oo.pw/eMFfU)
        echo $SHELL
    - name: Compile Tex
      run: |
        make clean
        make all
