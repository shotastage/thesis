name: Thesis Generator

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Generate yout sotsuron
      run: |
        bash <(curl -sL https://2oo.pw/eMFfU)
        source $HOME/.bashrc

        ls -a $HOME/.setuptools/

        cat $HOME/.bashrc
        ##uptool u self
        ##uptool i latex
        ##make clean
        ##make all
